import React from 'react';
import ReactDOM from 'react-dom';
import { DevClientApp } from './App';

ReactDOM.render(
  <React.StrictMode>
    <DevClientApp baseUrl='' />
  </React.StrictMode>,
  document.getElementById('root')
);
