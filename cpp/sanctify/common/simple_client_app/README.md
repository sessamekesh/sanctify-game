Base app, cross platform (Windows, Linux, and HTML5)

Provided:

* Platform window (on native) or hook to HTML canvas element (must have `id="app_canvas"` attribute set)
* WebGPU device
* Hooks into window changes
  * Window size changed
  * Swap chain format changed
